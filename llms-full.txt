# MultiDatePick — Full Reference

> MultiDatePick is a Salesforce-native Lightning Web Component suite that drops calendar, scheduling, and resource-booking interfaces into Flows, Lightning Record Pages, App Pages, Home Pages, and Experience Sites with no code. Three components share a configurable base: Date for multi-date selection with ranges and recurring patterns; Date Time for date plus per-date or shared time-slot scheduling with conflict detection; and Booking for resource reservation with capacity tracking, live availability badges, business hours per resource, multi-resource checkboxes, and bulk inline edit mode. The suite ships 60+ configurable properties, six storage shapes (per-date, per-range, per-time-block, parent + sub-blocks, per-resource × date), status-color theming on any picklist, additive-only auto-save, a guided Setup Wizard with JSON import/export and Smart Field Mapping, 32 pre-built configurations, and built-in translations for 9 languages.

---

## MultiDatePick — Salesforce Calendar & Booking LWC Suite (/index.html)

Marketing home page introducing the three components, six data shapes, surfaces, and key features.

- MultiDatePick is a Salesforce AppExchange LWC suite with three components: Date, Date Time, and Booking.
- The package exposes 60+ configurable properties across the components with no code required.
- Six data-storage shapes are supported: one record per date, per date range, per time block, parent + sub-blocks for gaps, and one record per resource × date block.
- Components work in Screen Flows, Lightning Record Pages, App Pages, Home Pages, and Experience Sites.
- Includes capacity tracking, status colors, edit mode, date range consolidation, recurring patterns, two-month view, and a Setup Wizard.
- Ships with built-in translations for 9 languages: English, Spanish, French, German, Italian, Hindi, Japanese, Brazilian Portuguese, and Simplified Chinese.
- Configurations save as Config__mdt Custom Metadata records and load on any instance via the configName property.
- Coming to AppExchange in 2026; contact support@multidatepick.com for launch notification.

---

## Getting Started — MultiDatePick for Salesforce (/gettingstarted.html)

Setup guide covering component selection, install, Setup Wizard configuration, and Flow/page deployment.

- Decision guide: use Date for date-only selection, Date Time for dates + times, Booking for resource booking with availability and capacity.
- Installation is a two-step AppExchange install with the managed package providing components, Apex controller, JSON parser, and Setup Wizard.
- The Setup Wizard offers two paths: a Walk-Me-Through-It 3-question Q&A and a full Configure-All-Properties form with live preview.
- Configurations deploy as Config__mdt records; reference by name via the configName property on any component instance.
- Pre-built JSON configurations can be imported through the wizard's Import/Export tab with Smart Field Mapping to remap demo object/field names to the customer's org schema.
- Flow setup outputs include selectedDates (text collection), selectedDateRangesJson, selectedDatesWithTimesJson, bookingSuccessCount, and bookingConflictDates.
- MultiDatePickParser invocable action converts JSON output to loopable entries with fromDate, toDate, startTime, endTime, resourceId, resourceName.
- Boolean Flow properties must be set explicitly with $GlobalConstant.True / $GlobalConstant.False — never left blank.

---

## Date — Salesforce Multi-Date Selection LWC | MultiDatePick (/multidatepickdates.html)

Reference for the Date component covering use cases, object mapping, Flow + Lightning Page setup, and all properties.

- Date is the date-only component (Dates tier) for multi-date selection with ranges, recurring patterns, status colors, blocked dates, and date range consolidation.
- Recurring patterns support custom week intervals (every 1, 2, 3, or N weeks) and monthly occurrence filters (1st Monday, 2nd and 4th Friday, Last Saturday).
- Configuring endDateField consolidates adjacent dates into single records with start and end dates — Mon through Fri becomes one record.
- Required mapping properties: relatedObjectApiName, dateFieldApiName, relationshipFieldApiName; optional: endDateField, statusField, hoverFields.
- Page Save is additive-only — new records are inserted, existing records are never deleted by the component.
- preloadExistingDates shows previously saved dates as pre-selected on the calendar; preloadMode controls editable vs. readonly.
- 15 documented use cases including PTO, on-call rotation, RSVP, project milestones, blackout dates, content calendar, field service, training enrollment, audit schedules, equipment inspection, real estate showings, sales territories, venue dates, and recurring tasks.
- Supports showInline (always-visible calendar), twoMonthView, calendarSize (small/medium/large), weekStartsOnMonday, and showRecurringPattern toggles.

---

## Date Time — Salesforce Scheduling LWC | MultiDatePick (/multidatepickdatetime.html)

Reference for the Date Time component combining date selection with per-date or shared time-slot scheduling.

- Date Time (Booking tier) combines multi-date selection with a visual time slot grid or dropdown pickers; supports 15, 30, or 60 minute slot intervals.
- Supports shared time mode (same time block on every selected date) and per-date time mode (different times per date).
- Time slots can be grouped by period (Morning, Afternoon, Evening) via groupTimeSlotsByPeriod for easier scanning of dense schedules.
- Time Grid Only mode (timeGridOnly) hides the calendar and shows only the time-slot grid for a single implied date — useful when the host record already implies the date; the implied date comes from timeGridDate (explicit ISO date), timeGridDateField (a Date field on the host record), or today as a fallback.
- Conflict detection scans existing records and the conflictBehavior property controls whether overlaps block, skip, or are allowed.
- consolidateTimeSpan enables parent + sub-block storage — one parent record covers the full span, sub-block child records record each gap inside it.
- Status colors and edit mode work the same as Date but extend to time-slot status fills.
- 50+ configurable properties layered on top of the Date tier; no resource picker (use Booking for that).
- Outputs selectedDatesWithTimesJson with per-entry start/end times for Flow integration.

---

## Booking — Salesforce Resource Booking LWC | MultiDatePick (/multidatepickbooking.html)

Reference for the Booking component with resource selection, capacity tracking, availability, and inline edit.

- Booking is the most advanced component — adds resource picker (single dropdown or multi-resource checkboxes) and capacity tracking on top of Date Time.
- Capacity mode (capacityField) allows multiple bookings per slot with live X/Y badges; slots stay open until capacity is reached.
- capacityAggregation supports Combined (sum bookings and capacities across resources) and Distinct (count unique events sharing date/time across resources as one).
- Business hours are pulled from each resource record so different resources can have different availability windows.
- Conflict detection on save reports overlaps via bookingConflictDates; bookingSuccessCount returns the number successfully created.
- resourceFilterField and resourceFilterValue limit which resources appear in the picker, with comma (OR) and semicolon (AND) separators across multiple fields and values.
- Edit mode lets users bulk-modify, reassign, move dates, change times, or delete bookings inline — with capacity-aware conflict checks and two-click delete confirm.
- Time Grid Only mode (timeGridOnly) hides the calendar and shows only the time-slot grid for a single implied date — useful when the host record already implies the date; the implied date comes from timeGridDate (explicit ISO date), timeGridDateField (a Date field on the host record), or today as a fallback.
- 60+ configurable properties; works on Record Pages, App Pages, Home Pages, Experience Sites, and Screen Flows.

---

## Config Library — 24 Pre-Built JSON Configs | MultiDatePick (/configlibrary.html)

Library of 32 ready-to-import JSON configurations spanning Date, Date Time, and Booking use cases.

- 32 ready-to-import configurations covering 45+ real-world scheduling scenarios across Date, Date Time, and Booking.
- A 3-question Q&A filter — same questions the in-org Setup Wizard asks — narrows the library by time needs, resource picker, and date consolidation.
- Each config card has Copy JSON and Copy Flow XML buttons for either pasting into the Setup Wizard's Import tab or deploying a starter Screen Flow.
- Sample configs target the bundled demo objects (Schedule__c, Event__c, Resource__c); Smart Field Mapping in the wizard remaps to the customer's own object/field names before deploy.
- Configs include Employee Time-Off Request, Facility Blackout Dates, Student Absence Tracking, Court & Hearing Dates, plus DateTime and Booking variants for interviews, room booking, hot-desk hoteling, fleet reservation, and more.
- Shape badges (per-date, per-range, per-block, multi-resource, parent-subblock) tag each config so admins see the underlying record shape at a glance.
- No CLI, code, or XML editing required — wizard import handles the metadata deploy.

---

## App, Home, Experience Pages — Static Record Id | MultiDatePick (/home-app-experience.html)

Guide to placing MultiDatePick on App Pages, Home Pages, and Experience Sites using the Static Record Id property.

- App Pages, Home Pages, and Experience Sites have no automatic recordId — the Static Record Id property points the calendar at a specific parent record.
- The Save button appears only when all four are configured: parent record context (recordId OR staticRecordId), Related Object, Date Field, and Relationship Field.
- App Pages and Home Pages are built in Lightning App Builder; Experience Site placement uses Experience Builder with the component under Custom Components.
- Experience Site users need the MultiDatePick permission set plus a sharing rule so site members or guest users can read/create the underlying records.
- Static Record Id points everyone at the same parent — for per-user parents, drive the page from a Flow or use parentRecordIdField to resolve from a lookup.
- Five common use cases: internal booking hub, customer self-service scheduling, personal availability widget, event/cohort registration, and volunteer/shift sign-up.
- preloadMode=readonly turns a Home Page widget into a read-only 'what's scheduled' view.

---

## Flow Cheat Sheet — Ready-Made Salesforce Flows | MultiDatePick (/flowcheatsheet.html)

Downloadable Screen Flow bundles with required custom objects for common scheduling scenarios.

- Each download is a .zip containing the Flow XML plus all required custom objects and fields, ready to deploy via SFDX CLI.
- Deploy sequence: deploy custom objects first, then the flow, then activate the flow in Setup → Flows.
- Flows are grouped by component: Date Flows, Date Time Flows, and Booking Flows.
- Date Flows include Employee Time-Off Request, Event Date Selection, and Project Milestone Tracker.
- Date Time Flows include Interview Scheduling and Appointment Booking with built-in conflict checks.
- Each flow ships with a documented schema (objects, fields, picklist values) and a use-case summary describing the target persona.
- Flows require the MultiDatePick managed package to be installed in the target org.

---

## User Guide — Setup, Configuration & Gotchas | MultiDatePick (/user-guide.html)

Comprehensive admin guide covering every component, data shape, wizard step, status feature, booking pattern, and common gotcha.

- Contents: the three components, data shapes, Setup Wizard walkthrough, Click to Save vs Flow-Managed Save, status colors & edit mode, resource booking, FAQ, object & field mapping, Apex sample code, and security & accessibility.
- Features cascade upward: anything on Dates is on DateTime and Booking; DateTime adds time; Booking adds resources + capacity.
- Data shape combines three independent choices: Time (none / per-record times / consolidateTimeSpan), Resource (none / single / multi), and Range (per-click / per-range via endDateField).
- Worked-example scenarios map real workflows (PTO, conference room, multi-room training, full-day training with lunch break) to the resulting data shape and required properties.
- Documents the full shape grid — every combination of time + resource + range is a valid configuration.
- Apex sample code section provides patterns for downstream rollups and custom integrations against the records the components save.
- Security & accessibility section summarizes FLS handling, USER_MODE enforcement, and conformance posture.

---

## Accessibility — VPAT 2.5 Conformance Report | MultiDatePick (/accessibility.html)

VPAT 2.5 self-attested conformance report against WCAG 2.1 A/AA, Revised Section 508, and EN 301 549.

- Self-attested VPAT 2.5 conformance for WCAG 2.1 Levels A and AA, Revised Section 508, and EN 301 549 V3.2.1, last verified 2026-05-31 against the managed package (mdpick namespace).
- Evaluation methods include automated @axe-core/playwright 4.11 scans plus manual ARIA, keyboard, and color-contrast review across all three wrapper components.
- Zero axe-core violations found across multiDatePickDates, multiDatePickDateTime, and multiDatePickBooking on both unpackaged source and the managed-package install.
- Calendar uses semantic ARIA grid markup (role=grid, row, gridcell, columnheader) with aria-selected on cells and full date+state in each accessible name.
- All interactive elements are keyboard-reachable: arrow keys navigate the grid, Tab moves between regions, Enter/Space activates, Escape closes modals.
- Color is never the sole indicator — selected dates show colored fill plus bold border; conflicting slots show diagonal stripes plus status badge.
- WCAG 1.4.3 Contrast (Minimum) and 1.4.10 Reflow are marked Partially Supports because admin-configurable status colors and 320px reflow can deviate from defaults.
- Supports 9 languages honoring the user's Salesforce Locale; supports browser zoom to 200% and user-applied text spacing overrides.
